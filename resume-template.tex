\documentclass{article}

\usepackage{enumitem}
\usepackage{fontspec}

\setmainfont{Charter}

\begin{document}

\def \tableWidth{}
\NewDocumentCommand{\education}{mmmm}{
  \item{
    \begin{tabular*}{\tableWidth\textwidth}{l@{\extracolsep{\fill}}r}
      \textbf{#1} & \textbf{#2} \\
      \textit{\small #3} & \textit{\small #4} \\
    \end{tabular*}
  }
}

$if(person)$
\section*{Person}

\def \fullName{%
  $if(person.first_name)$$person.first_name$$endif$
  $if(person.last_name)$$person.last_name$$endif$
}

\textbf{Name:} \fullName \\
$if(person.city)$\textbf{City:} $person.city$$else$%$endif$ \\
$if(person.phone)$\textbf{Phone:} $person.phone$$else$%$endif$ \\
$if(person.email)$\textbf{Email:} $person.email$$else$%$endif$ \\
$if(person.website)$\textbf{Website:} $person.website$$else$%$endif$ \\
$endif$

$if(education)$
\section*{Education}
\begin{itemize}[leftmargin=-0.6em, label={}]
$for(education)$
\education
    {$education.institution$}
    {$education.end$}
    {$education.degree$}
    {$education.location$}
$endfor$
\end{itemize}
$endif$

\end{document}
